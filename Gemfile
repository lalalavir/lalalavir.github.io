source "https://rubygems.org"

# Mirrors the GitHub Pages build environment so local previews match production.
gem "github-pages", group: :jekyll_plugins

# Lock down ffi on newer Ruby for local builds.
gem "webrick", "~> 1.8"
